# Harness

Repository harness tooling: reference policy, project generation, and validation helpers.

- `reference_policy.py` — residual-reference scanning shared by tests and generation
- `generator/` — template rendering and clean-room validation

Authoritative entry point: `scripts/generate-project`.
