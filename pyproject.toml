[project]
name = "project-harness-bootstrap-kit"
version = "0.1.0"
description = "Reusable repository harness and bootstrap kit tooling (not a product runtime)"
readme = "README.md"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "detect-secrets>=1.5.0",
    "pre-commit>=4.0.0",
    "pytest>=8.0.0",
    "ruff>=0.9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.lint.per-file-ignores]
"tests/fixtures/architecture/cases/**/*.py" = ["F401", "F811"]

[tool.ruff.format]
quote-style = "double"
